#include <stdlib.h>
#include "types.h"
#include "lexer.h"
#include "parser.h"

pipeline_t *parser_parse(token_t *tokens) {
    (void)tokens;
    return NULL;
}

void parser_free(pipeline_t *pipeline) {
    (void)pipeline;
}
